# Mock TUI Client

## What Changed

- Added a `varuna-tui` binary scaffold with a lazygit-style mocked torrent dashboard.
- Added a reusable `varuna_tui` module under `src/tui/` with separate mock data, state, rendering, and terminal entry-point code.
- Added focused `zig build build-tui`, `zig build run-tui`, and `zig build test-tui` steps so the TUI can be built and exercised independently from daemon dependencies.
- Reworked the interactive client to run through ZigZag v0.1.3's `Program` model/update/view runtime.
- Added mock modal interaction for adding torrents, multi-mark selection styling, and pane-local navigation for filters and detail tabs.
- Added mocked torrent sorting, text filtering, and viewport-aware torrent/detail scrolling.
- Fixed constrained-height rendering so the frame honors the actual terminal height instead of forcing a 24-line layout that scrolls off-screen in short tmux panes.
- Added a visual polish pass with a btop-like dark palette, richer Unicode symbols, Nerd Font opt-in, and a settings modal for switching symbol sets.
- Split reusable TUI visual primitives into `src/tui/components.zig` so renderer code can use shared symbols, styles, modal lines, progress bars, and footer key pills.

## What Was Learned

- ZigZag v0.1.3 works with Zig 0.15.2 for this mock client, even though the package metadata still reports version `0.1.2`.
- The worktree's general `zig build test` path depends on system `sqlite3`; the focused TUI target avoids that daemon dependency.
- Observing the interactive client in tmux exposed terminal-specific issues that snapshots missed: ANSI escape bytes were counted as columns, raw mode disabled newline carriage returns, and full-height frames scrolled the pane.
- Moving to ZigZag removed the hand-written raw-mode terminal loop and gives us its `start()` / `tick()` path for later integration with daemon-style polling.
- Filter navigation can be modeled entirely in `AppState`: selecting the left-pane "Seeding" filter now narrows the mocked torrent table and keeps the selected torrent visible.
- The unreadable short-pane rendering was caused by the renderer clamping height upward to 24 rows; the fix is to keep the frame within the terminal and let the torrent/detail viewports scroll.
- ZigZag reports Ctrl-U in the filter modal as a modified character event, so the app-level key handler needs to inspect `key.modifiers.ctrl` rather than rely only on raw byte `0x15`.
- Nerd Font mode works best as an explicit runtime setting: Unicode remains the portable default, while private-use glyphs are available for terminals with a patched font.
- Keeping symbols/styles in a component module matches the ZigZag source layout better than continuing to grow `render.zig` with one-off helper functions.

## Remaining Issues

- The TUI uses mocked data only. The next integration step is a real RPC/HTTP client feeding the same `AppState` shape or a thin snapshot model.
- The renderer still builds a custom lazygit-style view string; future work can replace more of that with ZigZag layout/table components if those APIs fit the desired density.
- The add-torrent modal intentionally discards input on submit for now; it proves the modal/key flow without committing to an RPC shape.
- Sorting is currently keyboard-cycled (`s` for sort key, `S` for direction). A future real client may want per-column commands once the table column model is less ad hoc.
- Settings are currently in-memory only; persistence can wait until broader TUI preferences exist.
- Repo-wide tests were not completed in this environment because `sqlite3` was unavailable to Zig's system-library lookup.

## Key Code References

- `build.zig:158` - `varuna_tui` module registration.
- `build.zig:218` - `varuna-tui` executable and focused build/run steps.
- `build.zig.zon:37` - ZigZag v0.1.3 package dependency.
- `src/tui/app.zig:7` - ZigZag model/update/view runtime for the mock TUI.
- `src/tui/components.zig:1` - reusable visual symbols, styles, progress bars, modal lines, and footer key pills.
- `src/tui/main.zig:12` - snapshot mode before entering ZigZag runtime.
- `src/tui/render.zig:13` - pure frame renderer for testable terminal output.
- `src/tui/render.zig:16` - constrained-height frame sizing and viewport rendering.
- `src/tui/model.zig:60` - filter, sorting, marking, modal, scrolling, and mocked torrent state.
- `tests/tui_render_test.zig:4` - focused render/model tests.
